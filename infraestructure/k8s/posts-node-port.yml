apiVersion: v1
kind: Service
metadata:
  name: posts-node-port
spec:
  type: NodePort
  selector:
    app: posts
  ports:
  - name: posts-node-port-port
    protocol: TCP
    port: 4000
    targetPort: 4000
